import { ArrowRight, Check, Code2, FileText, Heart, MessageCircle, Search, ShieldCheck, Sparkles, Star, UserRound, WalletCards, X } from "lucide-react";
import { useEffect, useState } from "react";
import { Link, useRoute } from "wouter";
import { createProposal, supabase } from "@/lib/integrations";
import { toast } from "sonner";

const empty = (title: string, body: string, href = "/connexion", action = "Créer un compte") => <div className="rounded-[26px] border border-dashed border-ink/15 bg-white p-8 text-center"><span className="mx-auto grid h-12 w-12 place-items-center rounded-2xl bg-[#eaf0ff] text-[#1557f5]"><UserRound size={20} /></span><h2 className="mt-5 font-display text-xl font-bold">{title}</h2><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-ink/50">{body}</p><Link href={href} className="mt-5 inline-flex items-center gap-2 rounded-full bg-ink px-4 py-3 text-sm font-bold text-white">{action} <ArrowRight size={15} /></Link></div>;
const Intro = ({ eyebrow, title, body }: { eyebrow: string; title: React.ReactNode; body: string }) => <div><p className="eyebrow">{eyebrow}</p><h1 className="mt-3 max-w-3xl font-display text-4xl font-bold leading-[1.02] tracking-[-.06em] sm:text-6xl">{title}</h1><p className="mt-4 max-w-xl text-base leading-7 text-ink/55">{body}</p></div>;

type Profile = { id: string; full_name: string; username: string | null; role: string; avatar_url: string | null; bio: string | null; country: string | null; skills: string[]; availability: string };
type Project = { id: string; title: string; slug: string; description: string; category: string; budget_min: number | null; budget_max: number | null; currency: string; created_at: string };

function Avatar({ profile, large = false }: { profile: Pick<Profile, "full_name" | "avatar_url">; large?: boolean }) { return <div className={`overflow-hidden rounded-2xl bg-[#eaf0ff] ${large ? "h-24 w-24 text-3xl" : "h-16 w-16 text-lg"}`}>{profile.avatar_url ? <img src={profile.avatar_url} alt={`Photo de ${profile.full_name}`} className="h-full w-full object-cover" /> : <span className="grid h-full w-full place-items-center font-display font-bold text-[#1557f5]">{profile.full_name.split(" ").map((part) => part[0]).slice(0, 2).join("").toUpperCase() || "U"}</span>}</div>; }

 export function FindDeveloperPage() { const [profiles, setProfiles] = useState<Profile[]>([]); const [loading, setLoading] = useState(true); useEffect(() => { (async () => { if (!supabase) return setLoading(false); const { data } = await supabase.from("profiles").select("id, full_name, username, role, avatar_url, bio, country, skills, availability").eq("role", "developer").eq("availability", "available").order("created_at", { ascending: false }).limit(30); setProfiles((data ?? []) as Profile[]); setLoading(false); })(); }, []); return <main className="container py-10 sm:py-16"><Intro eyebrow="Trouver un développeur" title={<>Le bon profil pour faire passer votre projet au <span className="text-coral">niveau supérieur.</span></>} body="Découvrez uniquement les professionnels inscrits sur DevConnect, avec leurs compétences et leurs informations publiques." /><div className="mt-10 flex gap-3"><label className="relative flex-1"><Search className="absolute left-4 top-1/2 -translate-y-1/2 text-ink/35" size={18} /><input className="field bg-white pl-11" placeholder="Rechercher une compétence..." /></label><Link href="/publier" className="hidden items-center gap-2 rounded-full bg-ink px-5 py-3 text-sm font-bold text-white sm:inline-flex">Publier un projet <ArrowRight size={15} /></Link></div><div className="mt-10">{loading ? <p className="text-sm text-ink/45">Chargement des profils...</p> : profiles.length === 0 ? empty("Aucun développeur disponible pour le moment", "Les profils apparaîtront ici dès que de vrais développeurs auront terminé leur inscription.", "/connexion", "Je suis développeur") : <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{profiles.map((profile) => <article key={profile.id} className="rounded-[24px] border border-ink/[.08] bg-white p-5 shadow-[0_6px_24px_rgba(19,28,35,.04)]"><div className="flex items-start justify-between"><Avatar profile={profile} /><button className="rounded-full p-2 text-ink/25" aria-label="Ajouter aux favoris"><Heart size={18} /></button></div><h2 className="mt-5 font-display text-lg font-bold">{profile.full_name}</h2><p className="mt-1 text-sm text-ink/50">{profile.bio || "Développeur sur DevConnect"}</p><div className="mt-4 flex flex-wrap gap-1.5">{(profile.skills ?? []).slice(0, 4).map((skill) => <span key={skill} className="rounded-md bg-[#fbfaf7] px-2 py-1 text-[11px] font-semibold text-ink/55">{skill}</span>)}</div><div className="mt-5 flex items-center justify-between border-t border-ink/10 pt-4 text-xs text-ink/45"><span>{profile.country || "Pays non renseigné"}</span><span className="flex items-center gap-1 text-[#318653]"><span className="h-1.5 w-1.5 rounded-full bg-[#318653]" />Disponible</span></div><Link href={`/profil/${profile.username || profile.id}`} className="mt-4 block rounded-xl bg-ink py-3 text-center text-sm font-bold text-white">Voir le profil</Link></article>)}</div>}</div></main>; }

export function ExploreProjectsPage() { const [projects, setProjects] = useState<Project[]>([]); const [loading, setLoading] = useState(true); useEffect(() => { (async () => { if (!supabase) return setLoading(false); const { data } = await supabase.from("projects").select("id, title, slug, description, category, budget_min, budget_max, currency, created_at").eq("status", "open").order("created_at", { ascending: false }).limit(30); setProjects((data ?? []) as Project[]); setLoading(false); })(); }, []); return <main className="container py-10 sm:py-16"><Intro eyebrow="Projets publiés" title={<>Des missions réelles pour des talents qui veulent <span className="text-coral">construire.</span></>} body="Les opportunités affichées sont publiées par des clients inscrits. Aucun projet fictif n'est présenté." /><div className="mt-10">{loading ? <p className="text-sm text-ink/45">Chargement des projets...</p> : projects.length === 0 ? empty("Aucun projet ouvert pour le moment", "Les nouvelles missions apparaîtront ici dès qu’un client aura publié son premier projet.", "/publier", "Publier un projet") : <div className="grid gap-4">{projects.map((project) => <article key={project.id} className="rounded-[24px] border border-ink/[.08] bg-white p-6 shadow-[0_6px_24px_rgba(19,28,35,.04)] sm:p-7"><span className="rounded-full bg-[#f2f0eb] px-3 py-1 text-[10px] font-bold uppercase tracking-[.1em] text-ink/50">{project.category}</span><h2 className="mt-4 font-display text-xl font-bold sm:text-2xl">{project.title}</h2><p className="mt-3 max-w-3xl text-sm leading-6 text-ink/55">{project.description}</p><div className="mt-5 flex flex-wrap items-center justify-between gap-4 border-t border-ink/10 pt-4"><span className="font-display font-bold">{project.budget_min || project.budget_max ? `${project.budget_min ?? ""} – ${project.budget_max ?? ""} ${project.currency}` : "Budget à définir"}</span><Link href={`/projet/${project.slug}`} className="inline-flex items-center gap-2 text-sm font-bold text-[#1557f5]">Voir le projet <ArrowRight size={15} /></Link></div></article>)}</div>}</div></main>; }

export function ProjectDetailPage() { return <main className="container py-10 sm:py-16">{empty("Le projet sera visible après sa publication", "Cette page ne présente aucun exemple inventé. Connectez-vous pour publier ou consulter des projets réels.", "/connexion", "Se connecter")}</main>; }
export function DeveloperProfilePage() { return <main className="container py-10 sm:py-16"><div className="mx-auto max-w-3xl">{empty("Profil non disponible", "Les profils sont créés par les développeurs eux-mêmes. Les avis, commentaires et réalisations seront affichés uniquement après une inscription réelle.", "/connexion", "Créer mon profil")}<section className="mt-6 rounded-[26px] border border-ink/10 bg-white p-6 sm:p-8"><div className="flex items-center gap-3"><MessageCircle className="text-[#1557f5]" size={21} /><h2 className="font-display text-xl font-bold">Commentaires et avis</h2></div><p className="mt-3 text-sm leading-6 text-ink/50">Une interface moderne sera disponible après les premières missions : note, commentaire, date, projet lié et réponse du développeur.</p></section></div></main>; }

export function BecomeDeveloperPage() { return <main className="container py-10 sm:py-16"><div className="mx-auto max-w-3xl"><Intro eyebrow="Je suis développeur" title={<>Présentez votre talent avec <span className="text-coral">clarté.</span></>} body="Créez un profil professionnel avec votre spécialité, vos compétences, votre expérience et votre disponibilité." /><div className="mt-10 space-y-4">{["Votre identité et votre pays", "Votre spécialité principale", "Vos compétences et votre expérience", "Votre disponibilité et vos liens professionnels"].map((item, index) => <div key={item} className="flex items-center gap-4 rounded-2xl border border-ink/10 bg-white p-5"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[#eaf0ff] font-display font-bold text-[#1557f5]">0{index + 1}</span><span className="font-semibold">{item}</span><Check className="ml-auto text-[#318653]" size={18} /></div>)}<Link href="/connexion" className="mt-4 inline-flex items-center gap-2 rounded-full bg-ink px-5 py-3.5 text-sm font-bold text-white">Commencer mon inscription <ArrowRight size={16} /></Link></div></div></main>; }

export function ClientDashboardPage() { return <main className="container py-10 sm:py-14"><Intro eyebrow="Espace client" title={<>Votre espace pour trouver un <span className="text-coral">développeur.</span></>} body="Publiez vos besoins et suivez vos échanges dans un espace privé." /><div className="mt-10">{empty("Votre tableau de bord est vide", "Vos projets, propositions et messages apparaîtront ici après votre inscription et votre première publication.", "/publier", "Publier un projet")}</div></main>; }
export function DeveloperDashboardPage() { 
  type Proposal = { id: string; project_id: string; status: string; amount: number; delivery_days: number; cover_letter: string };
  const [projects, setProjects] = useState<Project[]>([]); const [proposals, setProposals] = useState<Proposal[]>([]); const [selected, setSelected] = useState<Project | null>(null); const [coverLetter, setCoverLetter] = useState(""); const [amount, setAmount] = useState(""); const [deliveryDays, setDeliveryDays] = useState(""); const [loading, setLoading] = useState(true); const [sending, setSending] = useState(false); const [error, setError] = useState("");
  useEffect(() => { let mounted = true; (async () => { if (!supabase) return setLoading(false); const { data: { user } } = await supabase.auth.getUser(); if (!user) return setLoading(false); const [{ data: openProjects }, { data: mine }] = await Promise.all([supabase.from("projects").select("id, title, slug, description, category, budget_min, budget_max, currency, created_at").eq("status", "open").order("created_at", { ascending: false }).limit(40), supabase.from("proposals").select("id, project_id, status, amount, delivery_days, cover_letter").eq("developer_id", user.id).order("created_at", { ascending: false }).limit(100)]); if (mounted) { setProjects((openProjects ?? []) as Project[]); setProposals((mine ?? []) as Proposal[]); setLoading(false); } })(); return () => { mounted = false; }; }, []);
  const applied = new Set(proposals.map((proposal) => proposal.project_id));
  const submitProposal = async (event: React.FormEvent<HTMLFormElement>) => { event.preventDefault(); setError(""); if (!supabase || !selected) return; const numericAmount = Number(amount); const days = Number(deliveryDays); if (coverLetter.trim().length < 80) return setError("Présentez votre approche en au moins 80 caractères."); if (!Number.isFinite(numericAmount) || numericAmount <= 0) return setError("Indiquez un montant valide."); if (!Number.isInteger(days) || days < 1) return setError("Indiquez un délai en jours valide."); setSending(true); try { const { data: { user } } = await supabase.auth.getUser(); if (!user) { setError("Connectez-vous pour postuler à un projet."); return; } await createProposal({ project_id: selected.id, developer_id: user.id, cover_letter: coverLetter.trim(), amount: numericAmount, delivery_days: days }); setProposals((current) => [...current, { id: crypto.randomUUID(), project_id: selected.id, status: "pending", amount: numericAmount, delivery_days: days, cover_letter: coverLetter.trim() }]); toast.success("Votre candidature a été envoyée au client."); setSelected(null); setCoverLetter(""); setAmount(""); setDeliveryDays(""); } catch (caught) { setError(caught instanceof Error && caught.message.includes("duplicate") ? "Vous avez déjà postulé à ce projet." : caught instanceof Error ? caught.message : "Impossible d'envoyer votre candidature."); } finally { setSending(false); } };
  const formatBudget = (project: Project) => project.budget_min || project.budget_max ? `${project.budget_min ?? ""} – ${project.budget_max ?? ""} ${project.currency}` : "Budget à discuter";
  return <main className="container py-10 sm:py-14"><Intro eyebrow="Espace développeur" title={<>Trouvez une mission et <span className="text-coral">postulez.</span></>} body="Parcourez les projets réels publiés par les clients et envoyez une proposition directement depuis votre tableau de bord." /><div className="mt-10 grid gap-4 sm:grid-cols-3">{[{ Icon: Code2, label: "Projets ouverts", value: String(projects.length) }, { Icon: FileText, label: "Mes candidatures", value: String(proposals.length) }, { Icon: Star, label: "Avis reçus", value: "0" }].map(({ Icon, label, value }) => <div key={label} className="rounded-[22px] border border-ink/10 bg-white p-5"><div className="grid h-10 w-10 place-items-center rounded-xl bg-[#eaf0ff] text-[#1557f5]"><Icon size={18} /></div><p className="mt-5 text-sm text-ink/45">{label}</p><p className="mt-1 font-display text-4xl font-bold">{value}</p></div>)}</div><section className="mt-10"><div className="flex flex-col justify-between gap-3 sm:flex-row sm:items-end"><div><p className="eyebrow">Opportunités</p><h2 className="mt-2 font-display text-3xl font-bold tracking-[-.05em]">Projets ouverts</h2></div><Link href="/messages" className="inline-flex w-fit items-center gap-2 text-sm font-bold text-[#1557f5]">Ma messagerie <MessageCircle size={16} /></Link></div>{loading ? <p className="mt-6 text-sm text-ink/45">Chargement des projets...</p> : projects.length === 0 ? <div className="mt-6">{empty("Aucun projet ouvert", "Les nouvelles missions publiées par les clients apparaîtront ici.", "/projets", "Voir les projets")}</div> : <div className="mt-6 grid gap-4 lg:grid-cols-2">{projects.map((project) => <article key={project.id} className="rounded-[24px] border border-ink/[.08] bg-white p-6 shadow-[0_6px_24px_rgba(19,28,35,.04)]"><div className="flex items-start justify-between gap-3"><span className="rounded-full bg-[#f2f0eb] px-3 py-1 text-[10px] font-bold uppercase tracking-[.1em] text-ink/50">{project.category}</span>{applied.has(project.id) && <span className="rounded-full bg-[#edf8f0] px-3 py-1 text-[10px] font-bold text-[#318653]">Candidature envoyée</span>}</div><h3 className="mt-4 font-display text-xl font-bold">{project.title}</h3><p className="mt-3 line-clamp-3 text-sm leading-6 text-ink/55">{project.description}</p><div className="mt-5 flex items-center justify-between border-t border-ink/10 pt-4"><span className="font-display font-bold">{formatBudget(project)}</span><button disabled={applied.has(project.id)} onClick={() => { setSelected(project); setError(""); }} className="inline-flex items-center gap-2 rounded-full bg-ink px-4 py-2.5 text-xs font-bold text-white disabled:bg-[#edf8f0] disabled:text-[#318653]">{applied.has(project.id) ? "Déjà postulé" : "Postuler"} {!applied.has(project.id) && <ArrowRight size={14} />}</button></div></article>)}</div>}</section>{proposals.length > 0 && <section className="mt-12"><p className="eyebrow">Suivi</p><h2 className="mt-2 font-display text-3xl font-bold tracking-[-.05em]">Mes candidatures</h2><div className="mt-5 space-y-3">{proposals.map((proposal) => { const project = projects.find((item) => item.id === proposal.project_id); return <div key={proposal.id} className="flex flex-col gap-3 rounded-2xl border border-ink/10 bg-white p-4 sm:flex-row sm:items-center"><div className="min-w-0 flex-1"><p className="truncate font-semibold">{project?.title || "Projet publié"}</p><p className="mt-1 text-xs text-ink/45">{proposal.amount} · {proposal.delivery_days} jours</p></div><span className="w-fit rounded-full bg-[#fff7e9] px-3 py-1 text-xs font-bold capitalize text-[#a96b17]">{proposal.status === "pending" ? "En attente" : proposal.status}</span></div>; })}</div></section>}{selected && <div className="fixed inset-0 z-[60] grid place-items-center bg-ink/45 p-4 backdrop-blur-sm"><div className="max-h-[90vh] w-full max-w-xl overflow-y-auto rounded-[28px] bg-white p-6 shadow-2xl sm:p-8"><div className="flex items-start justify-between gap-4"><div><p className="eyebrow">Candidater</p><h2 className="mt-2 font-display text-2xl font-bold">{selected.title}</h2></div><button onClick={() => setSelected(null)} className="rounded-full p-2 text-ink/45 hover:bg-[#f2f0eb]" aria-label="Fermer"><X size={19} /></button></div><form onSubmit={submitProposal} className="mt-7 space-y-5"><label className="block"><span className="label">Votre proposition <span className="text-coral">*</span></span><textarea required value={coverLetter} onChange={(e) => setCoverLetter(e.target.value)} className="field min-h-[150px] resize-y" placeholder="Présentez votre expérience, votre approche et ce que vous allez livrer..." /><span className="mt-1 block text-right text-xs text-ink/35">{coverLetter.length}/80 caractères minimum</span></label><div className="grid gap-4 sm:grid-cols-2"><label className="block"><span className="label">Votre montant ({selected.currency}) <span className="text-coral">*</span></span><input required type="number" min="1" value={amount} onChange={(e) => setAmount(e.target.value)} className="field" placeholder="Ex. 1500" /></label><label className="block"><span className="label">Délai (jours) <span className="text-coral">*</span></span><input required type="number" min="1" value={deliveryDays} onChange={(e) => setDeliveryDays(e.target.value)} className="field" placeholder="Ex. 21" /></label></div>{error && <p role="alert" className="rounded-xl bg-[#fff1ed] px-4 py-3 text-sm font-semibold text-[#b64f2e]">{error}</p>}<button disabled={sending} className="inline-flex w-full items-center justify-center gap-2 rounded-full bg-ink px-5 py-3.5 text-sm font-bold text-white disabled:opacity-60">{sending ? "Envoi en cours..." : "Envoyer ma candidature"} {!sending && <ArrowRight size={16} />}</button></form></div></div>}</main>; }
export function HelpSecurityPage() { const cards = [{ Icon: ShieldCheck, title: "Profils authentiques" }, { Icon: MessageCircle, title: "Commentaires liés aux missions" }, { Icon: Code2, title: "Compétences déclarées" }]; return <main className="container py-10 sm:py-16"><Intro eyebrow="Aide et confiance" title={<>Une expérience claire, des échanges <span className="text-coral">professionnels.</span></>} body="DevConnect protège la qualité des profils, des projets et des commentaires grâce à des données liées aux comptes réels." /><div className="mt-10 grid gap-4 sm:grid-cols-3">{cards.map(({ Icon, title }) => <div key={title} className="rounded-[22px] border border-ink/10 bg-white p-6"><Icon className="text-[#1557f5]" size={22} /><h2 className="mt-7 font-display text-lg font-bold">{title}</h2><p className="mt-2 text-sm leading-6 text-ink/50">Cette fonctionnalité s’appuie sur les données réelles de votre compte.</p></div>)}</div></main>; }
